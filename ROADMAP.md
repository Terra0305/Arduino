# 기능 로드맵

수업 준비를 더 편하게 만들기 위한 아이디어 목록. 우선순위 순으로 정리.

## 1. 빈칸 문제 자동 생성 — ✅ 구현됨

완성된(정답) Arduino 코드를 주면, `pinMode` / `digitalWrite` / `digitalRead` / `delay()` / 비교문(`== HIGH`)
안의 학습 포인트가 되는 값을 자동으로 `____`로 뚫어준다. 손으로 빈칸을 만드는 수고를 줄이는 게 목적.

- 구현: [`scripts/make-blanks.mjs`](scripts/make-blanks.mjs)
- 실행: `npm run blanks -- <코드 파일 경로>` (인자 없이 실행하면 사용법이 나온다)
- 난이도 3단계를 한 번에 만들어준다 (인자 없이 실행 시 전부 출력, `--level=`로 하나만 뽑을 수도 있음):
  - `value` — HIGH/LOW, delay() 숫자, 비교값만 뚫는다 (가장 쉬움)
  - `pin` — value + pinMode/digitalWrite/digitalRead 안의 핀 변수도 뚫는다
  - `mode` — pin + pinMode 두 번째 인자(INPUT/OUTPUT/INPUT_PULLUP)도 뚫는다 (가장 어려움)
- 정답은 항상 코드에 실제로 나타나는 순서(왼쪽→오른쪽, 위→아래)로 나온다.
- 완벽하게 사람처럼 골라내진 못하는 휴리스틱 도구다 — 만들어진 초안을 보고 필요하면 손으로 다듬는다.
- 채팅으로 "이 코드 완성본으로 2/3/4단계 빈칸 버전 만들어서 올려줘" 라고 하면, 이 도구로 만든 뒤
  `scripts/admin-cli.mjs`로 바로 사이트에 반영하는 흐름으로 쓰면 된다.

## 2. 수업 복제(Duplicate)

새 수업을 만들 때마다 준비물 · 공지 · 연결 방법 그림을 매번 새로 입력하는 대신,
기존 수업을 복제해서 코드만 바꿔 새 수업으로 저장하는 기능.

## 3. 스냅샷 / 복원 CLI

`admin-cli.mjs`에 `snapshot <classId>` (현재 상태를 로컬 파일로 저장) / `restore <classId> <파일>`
(그 파일로 되돌리기) 명령을 추가. 수업 중 실수로 잘못된 코드를 올렸을 때 바로 복구할 수 있게.

## 4. (선택) 요일/시간 예약 자동 전환

"매주 화요일 3시엔 자동으로 5번 수업으로 바뀌게" 같은 스케줄링. 반이 여러 개거나 정기 수업이면
유용하지만, 별도 스케줄러(Vercel Cron 등)가 필요해 앞의 항목들보다 구현 비용이 크다.
